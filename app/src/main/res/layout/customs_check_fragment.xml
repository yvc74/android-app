<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <Button
        android:id="@+id/greenButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/centerOfParent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginEnd="8dp"
        android:backgroundTint="@color/green"
        android:text="@string/customs_check_button_green"
        android:textColor="@android:color/white" />

    <Button
        android:id="@+id/redButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignStart="@+id/centerOfParent"
        android:layout_marginStart="8dp"
        android:backgroundTint="@color/red"
        android:text="@string/customs_check_button_red"
        android:textColor="@android:color/white" />

    <View
        android:id="@+id/centerOfParent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_centerInParent="true" />

</RelativeLayout>
